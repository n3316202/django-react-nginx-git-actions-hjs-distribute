### 1. 배포환경을 위하여 settings 파일을 3개로 나눔

### 2. .env 파일을 만듦

### 3. docker 폴더를 만듦


### 3. 배포시 에러 - 아래 설정
ALLOWED_HOSTS = ["*"]




### 전체 폴더 구조
django-react-nginx-git-actions-hjs-distribute/
├── docker-compose.yml
├── docker/
│   └──nginx 
│   └──backend 
│   └──frontend 
├── backend/
│   ├── 
│   └── manage.py (및 Django 프로젝트)
├── frontend/
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       └── ...

2. 깃허브 올리기
.gitignore 추가